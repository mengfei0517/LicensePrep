"""Planner Agent placeholder."""
from __future__ import annotations

class PlannerAgent:
    def plan(self, start: str, duration_min: int = 45) -> dict:
        return {"route": [start], "duration_min": duration_min, "notes": "Planner placeholder"}
