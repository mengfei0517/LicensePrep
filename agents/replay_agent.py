"""Replay Agent placeholder."""
from __future__ import annotations

class ReplayAgent:
    def analyze(self, gpx_path: str) -> dict:
        return {"summary": "Replay analysis placeholder", "path": gpx_path}
